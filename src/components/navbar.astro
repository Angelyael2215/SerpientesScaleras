<nav class="navbar">
  <div class="nav-container">
    <a href="/" class="logo">
      <span class="logo-text">Serpientes y Escaleras</span>
    </a>

    <div class="nav-links">
      <a href="/Alpha" class="link alfa">Nivel Alfa (6-8 años)</a>
      <a href="/Beta" class="link beta">Nivel Beta (9-12 años)</a>
    </div>
  </div>
</nav>

<style>
  .navbar {
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    padding: 1rem 2rem;
    position: sticky;
    top: 0;
    z-index: 100;
    border-bottom: 3px solid transparent;
    border-image: linear-gradient(90deg, #FF6B6B, #FFE066, #51CF66, #339AF0, #CC5DE8) 1;
  }

  .nav-container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo {
    font-size: 1.5rem;
    font-weight: 700;
    text-decoration: none;
    display: flex;
    align-items: center;
    gap: 10px;
    transition: transform 0.3s ease;
  }

  .logo:hover {
    transform: scale(1.05);
  }


  .logo-text {
    background: linear-gradient(90deg, #FF6B6B, #FF922B, #FFE066, #51CF66, #339AF0, #CC5DE8);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: rainbowText 4s linear infinite;
  }

  @keyframes rainbowText {
    0% { background-position: 0% center; }
    100% { background-position: 200% center; }
  }

  .nav-links {
    display: flex;
    gap: 15px;
  }

  .link {
    text-decoration: none;
    padding: 0.7rem 1.4rem;
    border-radius: 50px;
    font-weight: 600;
    font-size: 1rem;
    transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.3s ease;
    border: none;
  }

  .link:hover {
    transform: scale(1.1) translateY(-3px);
  }

  .alfa {
    background: linear-gradient(135deg, #FF922B, #FFD43B);
    color: #7c2d12;
    box-shadow: 0 4px 12px rgba(255, 146, 43, 0.4);
  }

  .alfa:hover {
    box-shadow: 0 8px 25px rgba(255, 146, 43, 0.6);
  }

  .beta {
    background: linear-gradient(135deg, #22D3EE, #3B82F6);
    color: #fff;
    box-shadow: 0 4px 12px rgba(59, 130, 246, 0.4);
  }

  .beta:hover {
    box-shadow: 0 8px 25px rgba(59, 130, 246, 0.6);
  }

  @media (max-width: 600px) {
    .nav-container {
      flex-direction: column;
      gap: 1rem;
    }
    .nav-links {
      flex-direction: column;
      width: 100%;
      align-items: center;
    }
  }
</style>
